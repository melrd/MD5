
/** En paramètre
les A B C D
*/

void round1(){
/** a = b + ((a + F(b,c,d) + X[k] + T[i]) <<< s) */
    for (int i = 0;i < 16; i++)
    {
        //formule via le tabRound
        //décalage
    }
}


void round2(){
/** a = b + ((a + G(b,c,d) + X[k] + T[i]) <<< s) */
    for (int i = 0;i < 16; i++)
    {
        //formule via le tabRound2
        //décalage
    }
}

void round3(){
/** a = b + ((a + H(b,c,d) + X[k] + T[i]) <<< s)*/
    for (int i = 0;i < 16; i++)
    {
        //formule via le tabRound3
        //décalage
    }
}

void round4(){
/** a = b + ((a + I(b,c,d) + X[k] + T[i]) <<< s) */
    for (int i = 0;i < 16; i++)
    {
        //formule via le tabRound4
        //décalage
    }
}
